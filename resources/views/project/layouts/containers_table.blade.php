<table class="table table-striped containers_group" id="containers_group_{{ $group->id }}">
    <thead>
    <tr>
        <th class="no-sort" style="width: 1%">
            #
        </th>
        <th style="width: 17%">
            {{ __('container.number') }}
        </th>
        <th class="no-sort" style="width: 20%">
            {{ __('container.project_dates') }}
        </th>
        <th class="no-sort" style="width: 30%">
            {{ __('container.for_client_and_us') }}
        </th>
        <th class="no-sort" style="width: 15%">
            {{ __('container.location') }}
        </th>
        <th class="no-sort">
            {{ __('container.return') }}
        </th>
    </tr>
    </thead>
    <tbody>
    </tbody>
</table>
